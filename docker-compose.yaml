version: '3'
services:
  not_gate:
    build: .
    environment:
      LISTEN_ADDR: "0.0.0.0:8080"
      GATE_TYPE: not
      OUTPUT_TYPE: stdout
    healthcheck:
      test:
        [
          "CMD",
          "curl",
          "-f",
          "http://127.0.0.1:8080/healthcheck"
        ]
    ports:
      - 8080:8080
  and_gate:
    build: .
    environment:
      LISTEN_ADDR: "0.0.0.0:80"
      GATE_TYPE: and
      OUTPUT_TYPE: http
      OUTPUT_ADDRS: 'http://not_gate:8080/input/a'
    healthcheck:
      test:
        [
          "CMD",
          "curl",
          "-f",
          "http://127.0.0.1:8080/healthcheck"
        ]
    ports:
      - 8081:8080
